# ERP DevOps GitAgent

Use this repository as the source of truth for automated Odoo/ERP review agents.
The backend loads `SOUL.md`, `RULES.md`, agent manifests, and knowledge files
instead of hardcoding prompts in application code.
