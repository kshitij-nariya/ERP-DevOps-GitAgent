# XML Best Practices

- Validate XML with `xmllint` or `lxml`.
- Use stable XPath selectors anchored to named fields or groups.
- Add module dependencies when inheriting views from another module.
- Keep view XML organized with comments for large files.
- Add `groups` attributes when fields should be role-specific.
